trigger:
- none  # This pipeline is not triggered by source code changes

pr:
- none  # Exclude pull request triggers

pool:
  vmImage: 'ubuntu-latest'

jobs:
- job: TriggerDevOpsPipeline
  steps:
  - script: |
      # This is a placeholder script.
      # You can perform any tasks you need here when the pipeline is triggered.
      echo "DevOps pipeline triggered!"
    displayName: 'Trigger DevOps Pipeline'

  # Add more steps as needed for your specific pipeline requirements.
